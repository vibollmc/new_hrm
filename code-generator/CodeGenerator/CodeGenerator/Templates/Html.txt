<hris-toolbar [buttons]="'add,edit,delete'"
              (onAddClick)="openAddOrEdit('add')"
              (onDeleteClick)="delete()"
              (onEditClick)="openAddOrEdit('edit')"
              [disableEdit]="!vm.objSelected.id"
              [disableDelete]="!vm.objSelected.id">
</hris-toolbar>
<kendo-dialog *ngIf="vm.isAddingOrEditing" (close)="closeAddOrEdit()" [minWidth]="500" [width]="600">
  <kendo-dialog-titlebar>
    <div style="font-size: 18px; line-height: 1.3em;">
      <span [ngClass]="classAddOrEdit"></span> {{titleAddOrEdit}}
    </div>
  </kendo-dialog-titlebar>
  <form class="k-form" #editForm="ngForm" (ngSubmit)="save()">
    <hris-textbox fieldname="Tên tiếng Việt" [(ngModel)]="vm.obj.name" required name="name"></hris-textbox>
    <hris-textbox fieldname="Tên tiếng Anh" [(ngModel)]="vm.obj.nameEn" name="nameEn"></hris-textbox>
    <div class="text-right dialog-button-bar">
      <button type="button" class="k-button" (click)="closeAddOrEdit()">Cancel</button>
      <button type="button" class="k-button k-primary" (click)="save()" [disabled]="!editForm.form.valid">Save</button>
    </div>
  </form>
</kendo-dialog>
<kendo-grid [kendoGridBinding]="vm.lstObj"
            [selectable]="true"
            [pageSize]="20"
            [pageable]="true"
            [filterable]="true"
            [height]="heightBrowserWithoutToolbar"
            (selectionChange)="gridSelectionChange($event)">
  <kendo-grid-column field="name" title="Tên tiếng Việt">
  </kendo-grid-column>
  <kendo-grid-column field="nameEn" title="Tên tiếng Anh">
  </kendo-grid-column>
  <kendo-grid-column field="status" title="Trạng thái" width="80" [filterable]="false">
    <ng-template kendoGridCellTemplate let-dataItem>
      <kendo-switch [(ngModel)]="dataItem.status" (valueChange)="updateStatus(dataItem)"></kendo-switch>
    </ng-template>
  </kendo-grid-column>
</kendo-grid>
